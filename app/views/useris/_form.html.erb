<%= form_with(model: useri, local: true) do |form| %>
  <% if useri.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(useri.errors.count, "error") %> prohibited this useri from being saved:</h2>

      <ul>
      <% useri.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
